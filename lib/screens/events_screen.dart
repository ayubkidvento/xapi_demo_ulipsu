import 'package:flutter/material.dart';

class EvenetsScreen extends StatelessWidget {
  const EvenetsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Evenets Screen'),
    );
  }
}